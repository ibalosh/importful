<nav class="navbar navbar-expand-lg mb-4 border-bottom border-info">
  <div class="container-fluid">
    <%= link_to image_tag("logo.svg", alt: "Importful Logo"), root_path, class: "navbar-brand pe-4" %>

    <button class="navbar-toggler" type="button"
            data-bs-toggle="collapse"
            data-bs-target="#navbarNav"
            aria-controls="navbarNav"
            aria-expanded="false"
            aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">

      <% if logged_in? %>
        <%= render "shared/navbar/current_user" %>
        <%= render "shared/navbar/app_items" %>
      <% else %>
        <%= render "shared/navbar/public_items" %>
      <% end %>
    </div>
  </div>
</nav>